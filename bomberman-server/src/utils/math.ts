export function tenFloor(i: number) {
  return Math.floor(i / 10) * 10;
}

export function tenCeil(i: number) {
  return Math.ceil(i / 10) * 10;
}

export function tenRound(i: number) {
  return Math.round(i / 10) * 10;
}